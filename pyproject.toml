[project]
name = "harness"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pedro Ferreira", email = "pedromoroferreira@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ddgs>=9.16.0",
    "llama-index>=0.14.24",
    "llama-index-llms-ollama>=0.10.1",
    "python-weather>=2.2.3",
    "torch>=2.14.0",
]

[project.scripts]
harness = "harness:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
